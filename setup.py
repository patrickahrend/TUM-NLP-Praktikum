from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='NLP Project for text classification based on relevance for several processes',
    author='Patrick Ahrend',
    license='',
)
